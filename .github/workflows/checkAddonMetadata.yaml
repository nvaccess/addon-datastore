name: Check submitted NVDA add-on

on:
  pull_request:
    branches:
      - master

jobs:

  checkMetadata:
  
    runs-on: windows-latest

    steps:
    
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
        submodules: true
    - name: Determine files changed
      uses: actions/github-script@v3
      with:
        script: |
          const restPrefix = "GET /repos/:owner/:repo/pulls/"
          const number = context.issue.number
          restSubfix = "/files"
          const url = restPrefix + number + restSubfix
          const files =await github.request(url)
          console.log(files)
